apiVersion: v1
kind: Service
metadata:
  name: lab-platform
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: lab-platform
  ports:
    - name: http
      port: 8080
      targetPort: 8080
